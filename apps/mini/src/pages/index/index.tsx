import { View, Text } from "@tarojs/components";

const IndexPage = () => {
  return (
    <View className="page">
      <View className="card">
        <Text className="title">小程序端基线已就绪</Text>
        <Text className="desc">这是 Taro + React + TypeScript 的首页占位。</Text>
      </View>
    </View>
  );
};

export default IndexPage;
